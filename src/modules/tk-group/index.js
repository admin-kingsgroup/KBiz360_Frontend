/* TK Group feature — public barrel (lazy-loaded by App.jsx via lazyModule).
   Exposes the routable page shells; the containers/api/utils they wrap live
   alongside and are covered by their own __tests__. */
export * from './pages';
export { StagePipeline } from './StagePipeline';
