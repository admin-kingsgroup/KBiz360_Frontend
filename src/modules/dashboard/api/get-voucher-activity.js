import {
  TODAY_VOUCHERS_BY_BR,
  RECENT_ACTIVITY_FEED,
} from '../../../core/helpers';

export const getTodayVouchersByBranch = async () => TODAY_VOUCHERS_BY_BR;

export const getRecentActivity = async () => RECENT_ACTIVITY_FEED;
